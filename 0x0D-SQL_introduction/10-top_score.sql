-- Script to list all records of a table of the database in MySQL server
-- Should display both score and name respectively and records to be ordered by score (top first)
SELECT score, name FROM second_table ORDER BY score DESC;
